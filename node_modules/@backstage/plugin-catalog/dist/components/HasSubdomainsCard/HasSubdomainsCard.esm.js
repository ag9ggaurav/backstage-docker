import { jsx } from 'react/jsx-runtime';
import { RELATION_HAS_PART } from '@backstage/catalog-model';
import { entityColumnPresets, EntityRelationCard } from '@backstage/plugin-catalog-react/alpha';
import { RelatedEntitiesCard } from '../RelatedEntitiesCard/RelatedEntitiesCard.esm.js';
import { domainEntityColumns, domainEntityHelpLink, asDomainEntities } from '../RelatedEntitiesCard/presets.esm.js';
import { catalogTranslationRef } from '../../alpha/translation.esm.js';
import { useTranslationRef } from '@backstage/core-plugin-api/alpha';

function isLegacyProps(props) {
  return "variant" in props || "columns" in props || "tableOptions" in props;
}
function HasSubdomainsCard(props) {
  const { t } = useTranslationRef(catalogTranslationRef);
  if (isLegacyProps(props)) {
    const {
      variant = "gridItem",
      title: title2 = t("hasSubdomainsCard.title"),
      columns = domainEntityColumns,
      tableOptions = {}
    } = props;
    return /* @__PURE__ */ jsx(
      RelatedEntitiesCard,
      {
        variant,
        title: title2,
        entityKind: "Domain",
        relationType: RELATION_HAS_PART,
        columns,
        asRenderableEntities: asDomainEntities,
        emptyMessage: t("hasSubdomainsCard.emptyMessage"),
        emptyHelpLink: domainEntityHelpLink,
        tableOptions
      }
    );
  }
  const {
    title = t("hasSubdomainsCard.title"),
    columnConfig = entityColumnPresets.domain.columns
  } = props;
  return /* @__PURE__ */ jsx(
    EntityRelationCard,
    {
      title,
      entityKind: "Domain",
      relationType: RELATION_HAS_PART,
      columnConfig,
      emptyState: {
        message: t("hasSubdomainsCard.emptyMessage"),
        helpLink: entityColumnPresets.domain.helpLink
      }
    }
  );
}

export { HasSubdomainsCard };
//# sourceMappingURL=HasSubdomainsCard.esm.js.map
