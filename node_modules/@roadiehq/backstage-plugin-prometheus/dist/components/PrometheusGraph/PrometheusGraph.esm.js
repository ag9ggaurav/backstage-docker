import { jsx, jsxs } from 'react/jsx-runtime';
import { useEffect } from 'react';
import { DateTime } from 'luxon';
import { Grid, Typography, useTheme, Box } from '@material-ui/core';
import { ResponsiveContainer, AreaChart, XAxis, YAxis, Tooltip, Area, ComposedChart, CartesianGrid, Legend, Line } from 'recharts';
import useDimensions from 'react-use-dimensions';
import { Progress, Link, InfoCard } from '@backstage/core-components';
import Alert from '@material-ui/lab/Alert';
import { CustomTooltip, tooltipCollector } from '../CustomTooltip.esm.js';
import { useMetrics } from '../../hooks/usePrometheus.esm.js';

const strokeColors = [
  "#8884d8",
  "#82ca9d",
  "#e74a3b",
  "#d873ea",
  "#eac473",
  "#0b4ff8",
  "#73ea79",
  "#681577",
  "#00ffb0",
  "#ff0000"
];
const useStyles = (theme) => ({
  axis: {
    fill: theme.palette.text.primary
  },
  container: {
    height: 450,
    width: 1200
  },
  cartesianGrid: {
    stroke: theme.palette.textVerySubtle
  },
  chart: {
    margin: {
      right: 30,
      top: 16
    }
  },
  yAxis: {
    width: 75
  }
});
const CustomizedTick = ({ x, y, payload, collector }) => {
  const formatter = new Intl.NumberFormat("en", {
    notation: "compact"
  });
  collector.collect(payload.value, y);
  return /* @__PURE__ */ jsx("g", { children: /* @__PURE__ */ jsx("text", { x, y, fill: "#5d6571", textAnchor: "end", dy: 16, children: formatter.format(payload.value) }) });
};
const AreaGraph = ({ data, keys, metrics }) => {
  const collector = tooltipCollector();
  const [chartRef] = useDimensions();
  const theme = useTheme();
  const styles = useStyles(theme);
  return /* @__PURE__ */ jsx(Box, { display: "flex", flexDirection: "column", ...{ ref: chartRef }, children: /* @__PURE__ */ jsx(
    ResponsiveContainer,
    {
      width: "95%",
      height: styles.container.height,
      className: "cost-overview-chart",
      children: /* @__PURE__ */ jsxs(AreaChart, { data, children: [
        /* @__PURE__ */ jsx("defs", { children: keys.map((key, idx) => /* @__PURE__ */ jsxs(
          "linearGradient",
          {
            id: `color${key}`,
            x1: "0",
            y1: "0",
            x2: "0",
            y2: "1",
            children: [
              /* @__PURE__ */ jsx(
                "stop",
                {
                  offset: "5%",
                  stopColor: strokeColors[idx % 10],
                  stopOpacity: 0.8
                }
              ),
              /* @__PURE__ */ jsx(
                "stop",
                {
                  offset: "95%",
                  stopColor: strokeColors[idx % 10],
                  stopOpacity: 0
                }
              )
            ]
          },
          key
        )) }),
        /* @__PURE__ */ jsx(
          XAxis,
          {
            dataKey: "time",
            domain: ["dataMin", "dataMax"],
            tickFormatter: (timeStr) => DateTime.fromSeconds(timeStr).toFormat("HH:mm:ss"),
            tickCount: 6,
            type: "number",
            stroke: styles.axis.fill
          }
        ),
        /* @__PURE__ */ jsx(YAxis, { tick: /* @__PURE__ */ jsx(CustomizedTick, { collector }) }),
        /* @__PURE__ */ jsx(
          Tooltip,
          {
            content: /* @__PURE__ */ jsx(CustomTooltip, { collector, metrics })
          }
        ),
        keys.map((key, idx) => /* @__PURE__ */ jsx(
          Area,
          {
            isAnimationActive: false,
            type: "monotone",
            dataKey: key,
            stroke: strokeColors[idx % 10],
            fillOpacity: 1,
            strokeWidth: 2,
            fill: `url(#color${key})`
          },
          key
        ))
      ] })
    }
  ) });
};
const LineGraph = ({ data, keys, metrics }) => {
  const collector = tooltipCollector();
  const [chartRef] = useDimensions();
  const theme = useTheme();
  const styles = useStyles(theme);
  return /* @__PURE__ */ jsx(Box, { display: "flex", flexDirection: "column", ...{ ref: chartRef }, children: /* @__PURE__ */ jsx(
    ResponsiveContainer,
    {
      width: "95%",
      height: styles.container.height,
      className: "cost-overview-chart",
      children: /* @__PURE__ */ jsxs(ComposedChart, { margin: styles.chart.margin, data, children: [
        /* @__PURE__ */ jsx(CartesianGrid, { stroke: styles.cartesianGrid.stroke }),
        /* @__PURE__ */ jsx(
          XAxis,
          {
            dataKey: "time",
            domain: ["dataMin", "dataMax"],
            tickFormatter: (timeStr) => DateTime.fromSeconds(timeStr).toFormat("HH:mm:ss"),
            tickCount: 6,
            type: "number",
            stroke: styles.axis.fill
          }
        ),
        /* @__PURE__ */ jsx(YAxis, { tick: /* @__PURE__ */ jsx(CustomizedTick, { collector }) }),
        /* @__PURE__ */ jsx(
          Tooltip,
          {
            content: /* @__PURE__ */ jsx(CustomTooltip, { collector, metrics })
          }
        ),
        /* @__PURE__ */ jsx(Legend, {}),
        keys.map((key, idx) => /* @__PURE__ */ jsx(
          Line,
          {
            dot: false,
            isAnimationActive: false,
            label: false,
            strokeWidth: 2,
            dataKey: key,
            stroke: strokeColors[idx % 10]
          },
          key
        ))
      ] })
    }
  ) });
};
const PrometheusGraph = ({
  query = `up`,
  range = { hours: 1 },
  step = 14,
  dimension,
  graphType = "line",
  title
}) => {
  const { fetchGraph, value, loading, error } = useMetrics({
    query,
    range,
    step,
    dimension
  });
  useEffect(() => {
    fetchGraph();
  }, [fetchGraph]);
  if (loading) {
    return /* @__PURE__ */ jsx(Progress, {});
  } else if (error) {
    return /* @__PURE__ */ jsx(Alert, { severity: "error", children: error.message });
  }
  const titleComponent = /* @__PURE__ */ jsxs(Grid, { container: true, justifyContent: "space-between", alignItems: "center", children: [
    /* @__PURE__ */ jsx(Grid, { item: true, children: title || query }),
    value?.uiUrl && /* @__PURE__ */ jsx(Grid, { item: true, children: /* @__PURE__ */ jsx(Typography, { variant: "subtitle1", children: /* @__PURE__ */ jsx(
      Link,
      {
        to: `${value.uiUrl}/graph?g0.expr=${encodeURI(query)}&g0.tab=0`,
        children: "Go to Prometheus"
      }
    ) }) })
  ] });
  const { data, keys, metrics } = value ?? { data: [], keys: [], metrics: {} };
  return graphType === "line" ? /* @__PURE__ */ jsx(InfoCard, { title: titleComponent, children: /* @__PURE__ */ jsx(LineGraph, { data, keys, metrics }) }) : /* @__PURE__ */ jsxs(InfoCard, { title: titleComponent, children: [
    " ",
    /* @__PURE__ */ jsx(AreaGraph, { data, keys, metrics })
  ] });
};

export { LineGraph, PrometheusGraph };
//# sourceMappingURL=PrometheusGraph.esm.js.map
