import { createPlugin, createApiFactory, fetchApiRef, discoveryApiRef } from '@backstage/core-plugin-api';
import { DoraMetricsClient, doraMetricsApiRef } from './api/DoraMetricsClient.esm.js';

const doraMetricsPlugin = createPlugin({
  id: "dora-metrics",
  apis: [
    createApiFactory({
      api: doraMetricsApiRef,
      deps: {
        discoveryApi: discoveryApiRef,
        fetchApi: fetchApiRef
      },
      factory: ({ discoveryApi, fetchApi }) => new DoraMetricsClient(discoveryApi, fetchApi)
    })
  ]
});

export { doraMetricsPlugin };
//# sourceMappingURL=plugin.esm.js.map
