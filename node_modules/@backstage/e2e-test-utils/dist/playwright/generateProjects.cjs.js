'use strict';

var fs = require('fs-extra');
var node_path = require('node:path');
var getPackages = require('@manypkg/get-packages');

function _interopDefaultCompat (e) { return e && typeof e === 'object' && 'default' in e ? e : { default: e }; }

var fs__default = /*#__PURE__*/_interopDefaultCompat(fs);

function generateProjects(options) {
  const { root, packages } = getPackages.getPackagesSync(process.cwd());
  const e2eTestPackages = [...root ? [root] : [], ...packages].filter((pkg) => {
    return fs__default.default.pathExistsSync(node_path.resolve(pkg.dir, "e2e-tests"));
  });
  return e2eTestPackages.map((pkg) => ({
    name: pkg.packageJson.name,
    testDir: node_path.resolve(pkg.dir, "e2e-tests"),
    use: {
      channel: options?.channel ?? "chrome"
    }
  }));
}

exports.generateProjects = generateProjects;
//# sourceMappingURL=generateProjects.cjs.js.map
