import { jsx, jsxs, Fragment } from 'react/jsx-runtime';
import { Progress, InfoCard, Table, Link, StatusAborted, StatusOK, StatusPending, StatusError } from '@backstage/core-components';
import { makeStyles, Grid, Typography, Chip, Tooltip } from '@material-ui/core';
import Alert from '@material-ui/lab/Alert';
import { DateTime } from 'luxon';
import { useAlerts } from '../../hooks/usePrometheus.esm.js';

const useStyles = makeStyles((theme) => ({
  chipRoot: {
    margin: theme.spacing(1),
    height: "100%"
  },
  chipLabel: {
    overflowWrap: "break-word",
    whiteSpace: "normal",
    textOverflow: "clip"
  }
}));
const state = (str) => {
  switch (str) {
    case "firing":
      return /* @__PURE__ */ jsx(StatusError, { children: str });
    case "pending":
      return /* @__PURE__ */ jsx(StatusPending, { children: str });
    case "inactive":
      return /* @__PURE__ */ jsx(StatusOK, { children: str });
    default:
      return /* @__PURE__ */ jsxs(StatusAborted, { children: [
        "Unknown (",
        str,
        ")"
      ] });
  }
};
const getColumns = (showAnnotations, showLabels) => {
  const columns = [
    {
      field: "name",
      title: "Name",
      render: (row) => /* @__PURE__ */ jsx(Tooltip, { title: `${row.query}`, children: /* @__PURE__ */ jsx("p", { children: row.name }) })
    },
    { field: "state", title: "State", render: (row) => state(row.state) },
    {
      field: "lastEvaluation",
      title: "Last Evaluation",
      render: (row) => DateTime.fromISO(row.lastEvaluation).toLocaleString(
        DateTime.DATETIME_SHORT_WITH_SECONDS
      )
    },
    {
      field: "value",
      title: "Value",
      render: (row) => {
        if (!row.value) {
          return /* @__PURE__ */ jsx("p", { children: "-" });
        }
        const formatter = new Intl.NumberFormat("en", {
          notation: "compact"
        });
        const shortVal = formatter.format(row.value);
        return /* @__PURE__ */ jsx(Tooltip, { title: row.value, children: /* @__PURE__ */ jsx("p", { children: shortVal }) });
      }
    }
  ];
  if (showLabels) {
    columns.push({
      field: "labels",
      title: "Labels",
      render: (row) => /* @__PURE__ */ jsx(Fragment, { children: Object.entries(row.labels).map(([k, v]) => /* @__PURE__ */ jsx(Chip, { label: `${k}: ${v}`, size: "small" }, k + v)) })
    });
  }
  if (showAnnotations) {
    columns.push({
      field: "annotations",
      title: "Annotations",
      render: (row) => /* @__PURE__ */ jsx(Fragment, { children: Object.entries(row.annotations).map(([k, v]) => /* @__PURE__ */ jsx(
        Chip,
        {
          label: `${k}: ${v}`,
          size: "small",
          classes: {
            root: useStyles().chipRoot,
            label: useStyles().chipLabel
          }
        },
        k + v
      )) })
    });
  }
  return columns;
};
const PrometheusAlertStatus = ({
  alerts,
  extraColumns,
  onRowClick,
  showAnnotations = true,
  showLabels = true,
  showInactiveAlerts = false
}) => {
  const { error, loading, displayableAlerts, uiUrl } = useAlerts(
    alerts,
    showInactiveAlerts
  );
  if (loading) {
    return /* @__PURE__ */ jsx(Progress, {});
  } else if (error) {
    return /* @__PURE__ */ jsx(Alert, { severity: "error", children: error?.message });
  }
  const statePriority = {
    firing: 0,
    pending: 1,
    inactive: 2
  };
  const sortedAlerts = [...displayableAlerts].sort((a, b) => {
    const aPriority = statePriority[a.state] ?? 99;
    const bPriority = statePriority[b.state] ?? 99;
    if (aPriority !== bPriority) {
      return aPriority - bPriority;
    }
    return a.name.localeCompare(b.name);
  });
  const title = /* @__PURE__ */ jsxs(Grid, { container: true, justifyContent: "space-between", alignItems: "center", children: [
    /* @__PURE__ */ jsx(Grid, { item: true, children: "Prometheus Alerts" }),
    uiUrl && /* @__PURE__ */ jsx(Grid, { item: true, children: /* @__PURE__ */ jsx(Typography, { variant: "subtitle1", children: /* @__PURE__ */ jsx(Link, { to: `${uiUrl}/alerts`, children: "Go to Prometheus" }) }) })
  ] });
  const columns = getColumns(showAnnotations, showLabels);
  return /* @__PURE__ */ jsx("div", { children: /* @__PURE__ */ jsx(InfoCard, { title, noPadding: true, children: /* @__PURE__ */ jsx(
    Table,
    {
      options: {
        search: false,
        paging: true,
        pageSize: 5,
        pageSizeOptions: [5, 10, 20, 50],
        toolbar: false
      },
      onRowClick: onRowClick ? (_, rowData) => onRowClick(rowData) : void 0,
      data: sortedAlerts,
      columns: extraColumns ? columns.concat(extraColumns) : columns
    }
  ) }) });
};

export { PrometheusAlertStatus };
//# sourceMappingURL=PrometheusAlertStatus.esm.js.map
