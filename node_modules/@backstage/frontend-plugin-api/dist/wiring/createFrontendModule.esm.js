import { resolveExtensionDefinitions } from './resolveExtensionDefinition.esm.js';

function createFrontendModule(options) {
  const { pluginId } = options;
  const { extensions } = resolveExtensionDefinitions(options.extensions ?? [], {
    namespace: pluginId,
    featureType: "Module"
  });
  return {
    $$type: "@backstage/FrontendModule",
    version: "v1",
    pluginId,
    featureFlags: options.featureFlags ?? [],
    if: options.if,
    extensions,
    toString() {
      return `Module{pluginId=${pluginId}}`;
    }
  };
}

export { createFrontendModule };
//# sourceMappingURL=createFrontendModule.esm.js.map
