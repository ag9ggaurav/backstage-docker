import { jsx } from 'react/jsx-runtime';
import { RELATION_HAS_PART } from '@backstage/catalog-model';
import { entityColumnPresets, EntityRelationCard } from '@backstage/plugin-catalog-react/alpha';
import { RelatedEntitiesCard } from '../RelatedEntitiesCard/RelatedEntitiesCard.esm.js';
import { componentEntityColumns, asComponentEntities } from '../RelatedEntitiesCard/presets.esm.js';
import { catalogTranslationRef } from '../../alpha/translation.esm.js';
import { useTranslationRef } from '@backstage/core-plugin-api/alpha';

function isLegacyProps(props) {
  return "variant" in props || "columns" in props || "tableOptions" in props;
}
function HasSubcomponentsCard(props) {
  const { t } = useTranslationRef(catalogTranslationRef);
  if (isLegacyProps(props)) {
    const {
      variant = "gridItem",
      title: title2 = t("hasSubcomponentsCard.title"),
      columns = componentEntityColumns,
      tableOptions = {},
      kind: kind2 = "Component"
    } = props;
    return /* @__PURE__ */ jsx(
      RelatedEntitiesCard,
      {
        variant,
        title: title2,
        entityKind: kind2,
        relationType: RELATION_HAS_PART,
        columns,
        asRenderableEntities: asComponentEntities,
        emptyMessage: t("hasSubcomponentsCard.emptyMessage"),
        emptyHelpLink: "https://backstage.io/docs/features/software-catalog/descriptor-format#specsubcomponentof-optional",
        tableOptions
      }
    );
  }
  const {
    title = t("hasSubcomponentsCard.title"),
    columnConfig = entityColumnPresets.component.columns,
    kind = "Component"
  } = props;
  return /* @__PURE__ */ jsx(
    EntityRelationCard,
    {
      title,
      entityKind: kind,
      relationType: RELATION_HAS_PART,
      columnConfig,
      emptyState: {
        message: t("hasSubcomponentsCard.emptyMessage"),
        helpLink: "https://backstage.io/docs/features/software-catalog/descriptor-format#specsubcomponentof-optional"
      }
    }
  );
}

export { HasSubcomponentsCard };
//# sourceMappingURL=HasSubcomponentsCard.esm.js.map
