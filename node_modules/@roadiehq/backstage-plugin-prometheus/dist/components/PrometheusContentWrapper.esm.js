import { jsx, jsxs } from 'react/jsx-runtime';
import { Grid } from '@material-ui/core';
import { Content, ContentHeader, SupportButton } from '@backstage/core-components';
import { useEntity, MissingAnnotationEmptyState } from '@backstage/plugin-catalog-react';
import { PrometheusGraph } from './PrometheusGraph/PrometheusGraph.esm.js';
import { isPrometheusGraphAvailable, isPrometheusAlertAvailable, PROMETHEUS_RULE_ANNOTATION, PROMETHEUS_ALERT_ANNOTATION, PROMETHEUS_PLUGIN_DOCUMENTATION } from './util.esm.js';
import 'nunjucks';
import '@backstage/catalog-model';
import { PrometheusAlertStatus } from './PrometheusAlertStatus/PrometheusAlertStatus.esm.js';

const PrometheusContentWrapper = ({
  step = 14,
  range = { hours: 1 },
  graphType,
  extraColumns,
  showAlertsAnnotations,
  showAlertsLabels
}) => {
  const { entity } = useEntity();
  const graphContent = isPrometheusGraphAvailable(entity);
  const alertContent = isPrometheusAlertAvailable(entity);
  if (!graphContent && !alertContent) {
    return /* @__PURE__ */ jsx(
      MissingAnnotationEmptyState,
      {
        annotation: [PROMETHEUS_RULE_ANNOTATION, PROMETHEUS_ALERT_ANNOTATION],
        readMoreUrl: PROMETHEUS_PLUGIN_DOCUMENTATION
      }
    );
  }
  const ruleTuples = graphContent ? entity.metadata.annotations[PROMETHEUS_RULE_ANNOTATION].split(",").map((it) => it.split("|")) : [];
  const alerts = alertContent ? entity.metadata.annotations[PROMETHEUS_ALERT_ANNOTATION].split(",") : [];
  const alertProp = alerts.length > 0 && alerts[0] === "all" ? "all" : alerts;
  return /* @__PURE__ */ jsxs(Content, { children: [
    /* @__PURE__ */ jsx(ContentHeader, { title: "Prometheus", children: /* @__PURE__ */ jsx(SupportButton, { children: "Plugin to show Prometheus graphs and alerts for the project" }) }),
    alertContent && /* @__PURE__ */ jsx(Grid, { container: true, spacing: 3, direction: "column", children: /* @__PURE__ */ jsx(Grid, { item: true, children: /* @__PURE__ */ jsx(
      PrometheusAlertStatus,
      {
        alerts: alertProp,
        extraColumns,
        showAnnotations: showAlertsAnnotations,
        showLabels: showAlertsLabels
      }
    ) }) }),
    graphContent && ruleTuples && /* @__PURE__ */ jsx(Grid, { container: true, spacing: 3, direction: "column", children: ruleTuples.map(([query, dimension]) => /* @__PURE__ */ jsx(Grid, { item: true, children: /* @__PURE__ */ jsx(
      PrometheusGraph,
      {
        dimension,
        query,
        range,
        step,
        graphType
      }
    ) }, query)) })
  ] });
};

export { PrometheusContentWrapper as default };
//# sourceMappingURL=PrometheusContentWrapper.esm.js.map
